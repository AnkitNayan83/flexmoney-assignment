const AuthLayout = ({ children }: { children: React.ReactNode }) => {
    return <div className="flex items-center justify-center bg-blue-500 h-[100vh]">{children}</div>;
};

export default AuthLayout;
